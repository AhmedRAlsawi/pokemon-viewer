import React from 'react';
import { AppDiv, AppText } from '../../styles';

const LoadingComp: React.FC = () => {
  return (
    <AppDiv>
      <AppText>Loading page...</AppText>
    </AppDiv>
  );
};

export { LoadingComp };
